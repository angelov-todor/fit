# Project Rules

## Before every commit and push

Run tests and build, and confirm both pass with zero errors:

```bash
npm test
npm run build
```

Do not commit or push if either fails. Fix the issue first.
